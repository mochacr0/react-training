import { ToastContainer } from "react-toastify";

const CustomizedToastContainer = () => {
    return <ToastContainer autoClose={2000} pauseOnHover={false} />;
};

export default CustomizedToastContainer;
